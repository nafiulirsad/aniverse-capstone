<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ backup policy. Nothing is copied off the device: the Room database is encrypted
    with a key that only exists inside this device's Keystore, so a restored copy could never be
    opened anyway, and the theme preference is not worth transferring.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
